#include "Game.h"

int main()
{

	/*Stage *stage = new Stage(grid);
	Player *p = new Player();
	std::vector<Stage*> st = { stage };
	Tower *t = new Tower(st);
	Game *g = new Game(p, t);

	g->play();
	*/
	return 0;
}